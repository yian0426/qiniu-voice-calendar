# 七牛云 前端开发项目

本项目主要完成了首页布局架构设计。

## 特性
- 前端使用 Vue 3 + Vite
- 将 axios 实例封装到了 `src/utils/request.ts` 中的 request 函数，不暴露单独的实例，只留出接口预留位置
- 完成了日程页面及聊天机器人页面的UI
- 左下角带有头像菜单展开功能，且包含了相关的 lucide 图标

## 运行
```bash
npm install
npm run dev
```
